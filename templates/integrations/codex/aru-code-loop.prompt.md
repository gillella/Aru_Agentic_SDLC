Continue only the Aru factory loop for this project:

  {{PROJECT}}

Use that repository's GitHub Project Board, AGENTS.md, and the canonical
Aru_Agentic_SDLC workflow (`run-aru-factory` loop). On every wake, recover
existing claims, worktrees, pull requests, reviews, CI, and merge-ready work
before claiming anything new.

If `$HOME/.aru/factory-loop.stop` applies to this project, stop and pause this
heartbeat. Do not operate on any other project. Do not replace this Codex
thread with a CLI agent.

This prompt is for a **thread** automation bound to the current Codex task.
Do not use it as a standalone (new-chat) automation.
