Implement the next actionable GitHub issue using Aru_Agentic_SDLC.

1. Read and follow `$ARU_SDLC_HOME/skills/implement-next-issue/SKILL.md`
   (or the Cursor skill `implement-next-issue`).
2. Use agent id `cursor-1` unless another id is already in use for this session.
3. Resume in-flight work for that agent before claiming anything new.
4. Work only inside `.worktrees/`, run local tests, open a PR with `Closes #<n>`.
