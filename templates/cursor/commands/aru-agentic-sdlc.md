Route this request to the correct Aru_Agentic_SDLC procedure.

1. Read and follow `$ARU_SDLC_HOME/skills/aru-agentic-sdlc/SKILL.md`.
2. Confirm `$ARU_SDLC_HOME` is set and the repo has an `AGENTS.md` with the
   Issue-First Law; if not, offer `init-agent-project`.
3. Dispatch to the matching skill rather than improvising the workflow.
